# Keep credentials out of source control. Copy this file outside the repository
# or load it through your deployment secret manager.
TUSHARE_TOKEN=replace_with_your_tushare_token
# Optional. Defaults to data/raw/provider_snapshots under the project root.
TRADINGOS_RAW_SNAPSHOT_DIR=replace_with_an_absolute_or_project_relative_directory
